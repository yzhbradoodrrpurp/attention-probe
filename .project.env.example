# Project runtime template.
# Copy to .project.env and fill local values when needed. Do not commit real values.

LOCAL_CONFIG_PATH=
LOCAL_TRAIN_ENTRY=

REMOTE_CONDA_ENV=
REMOTE_CONDA_INIT=
REMOTE_PYTHON=python
REMOTE_MAX_JOBS=16
REMOTE_GPU_IDS=
REMOTE_TMUX_SESSION=
REMOTE_TRAIN_CONFIG=
REMOTE_HF_ENDPOINT=

# Optional package index configuration for remote setup.
UV_INDEX_URL=
UV_DEFAULT_INDEX=
