# W&B template.
# Copy to .wandb.env and fill real values locally. Do not commit real values.

export WANDB_BASE_URL=
export WANDB_API_KEY=
export WANDB_PROJECT=attention
# Optional: offline, disabled, or online.
export WANDB_MODE=
